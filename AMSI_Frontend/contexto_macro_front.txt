================================================================================
  AMSI PROJECT — FRONTEND CONTEXTO MACRO
================================================================================

NOME DO PROJETO: AMSI Project
DESCRIÇÃO: Sistema de gestão financeira para associação de moradores.
           TCC do Pedro com aplicação real — não é exercício acadêmico.
STATUS: Backend MVP finalizado. Frontend MVP concluído e em produção.

--------------------------------------------------------------------------------
  STACK
--------------------------------------------------------------------------------

Frontend : React.js + Vite
Backend  : FastAPI (Python) + PostgreSQL 17.4 — FINALIZADO, não mexer
Auth     : JWT próprio (implementado no backend)
Deploy   : Vercel (frontend)
API Base : https://amsi-project-chzs.vercel.app

--------------------------------------------------------------------------------
  REPOSITÓRIOS
--------------------------------------------------------------------------------

Pedro (principal, deploy Vercel): https://github.com/PedroSchvartz/amsi_project
Lucas (fork, responsável pelo CSS): https://github.com/Thierry0liveira/amsi_project

O fork do Lucas foi mergeado no main do Pedro em 2026-05-06.
CSS e visual: versão do Lucas.
Lógica de negócio, auth, sessão: versão do Pedro.

--------------------------------------------------------------------------------
  ATENÇÃO — ARQUIVOS ÓRFÃOS
--------------------------------------------------------------------------------

A pasta AMSI_Frontend contém resquícios de scaffolding SvelteKit:
  .svelte-kit/, svelte.config.js, vite.config.ts, static/, tsconfig.json

O projeto roda em React — ignorar esses arquivos completamente.

--------------------------------------------------------------------------------
  ESTRUTURA DO FRONTEND
--------------------------------------------------------------------------------

Caminho  : C:\Codigos\AMSI_Project_Desenvolvimento\AMSI_Frontend

AMSI_Frontend
├── public/
│   ├── favicon.svg
│   └── icons.svg
├── src/
│   ├── App.jsx                  — roteamento principal + MonitorSessao + Spinner
│   ├── main.jsx                 — entry point
│   ├── index.css
│   ├── assets/
│   │   └── AMSI_Logo.png        — logo da associação
│   ├── components/              — componentes reutilizáveis
│   │   ├── AdminRoute.jsx       — proteção de rota admin
│   │   ├── PrivateRoute.jsx     — proteção de rota autenticada
│   │   ├── Layout.jsx           — topbar + menu + footer (substitui NavBar.jsx)
│   │   ├── NavBar.jsx           — NÃO USADO (substituído pelo Layout.jsx)
│   │   ├── Perfilpopup.jsx      — popup de perfil com temas
│   │   ├── Login.jsx
│   │   ├── Home.jsx             — usado diretamente na rota /home
│   │   ├── ClientRegister.jsx
│   │   ├── UserList.jsx
│   │   ├── UserRegister.jsx
│   │   └── News.jsx             — sem uso aparente
│   ├── pages/                   — wrappers de página (um por rota)
│   │   ├── ClientRegisterPage.jsx
│   │   ├── HomePage.jsx
│   │   ├── LancamentoPage.jsx
│   │   ├── ListaLancamentosPage.jsx
│   │   ├── LoginPage.jsx
│   │   ├── TrocarSenhaPage.jsx
│   │   ├── UserListPage.jsx
│   │   ├── UserRegisterPage.jsx
│   │   └── dashboard.jsx
│   ├── services/                — camada de comunicação com a API
│   │   ├── api.js               — todas as chamadas HTTP
│   │   ├── auth.js              — token, sessão, logout
│   │   └── loadingContext.jsx   — contexto global de loading
│   └── styles/                  — CSS por componente
│       ├── theme.css            — variáveis dos dois temas + reset global
│       ├── layout.css           — topbar, menu, footer
│       ├── forms.css            — estilos compartilhados de formulários
│       ├── lancamento.css
│       ├── listaLancamentos.css
│       ├── home.css
│       ├── login.css
│       ├── clientRegister.css
│       ├── userList.css
│       └── userregister.css
├── vite.config.js
├── package.json
├── contexto_macro_front.txt     — este arquivo
└── fase_atual_front.txt

--------------------------------------------------------------------------------
  SISTEMA DE TEMAS
--------------------------------------------------------------------------------

Dois temas implementados via variáveis CSS em styles/theme.css:
  - Verde (padrão): tons de verde escuro e bege
  - Corporativo: azul claro sobre fundo escuro

Variáveis principais:
  --bg, --bg-card, --primary, --secondary, --accent
  --text, --text-muted, --border, --input-bg, --btn-hover, --shadow

Alternado via botão no topbar do Layout.jsx.
Preferência salva em localStorage('amsi_tema').
Aplicado via data-theme="corporativo" no documentElement.

Todo CSS novo deve usar essas variáveis — nunca cores hardcoded.

--------------------------------------------------------------------------------
  CONTRATO COM O BACKEND
--------------------------------------------------------------------------------

Fonte da verdade da API: openapi_ai.yaml (raiz do projeto)
Swagger local: /docs (backend rodando)

Fluxo de autenticação (implementado):
  1. POST /auth/token → recebe { access_token, primeiro_acesso }
  2. Token salvo em localStorage('token')
  3. GET /usuarios/{sub} → dados completos salvos em localStorage('user')
  4. expiresAt salvo em localStorage('expiresAt') — timestamp ms
  5. Se primeiro_acesso == true → redireciona para /trocar-senha
  6. Se receber 401 → handleResponse() chama logout() e dispara evento sessao-expirada

Logout:
  - logoutUser() chama POST /auth/logout (invalida token no banco)
  - logout() de auth.js limpa localStorage
  - Sempre chamar logoutUser() antes de logout()

Regras de contrato:
  - Aceita e retorna JSON
  - Nunca retorna o campo `senha`
  - Datas em ISO 8601 (ex: "2026-04-19T10:00:00")
  - Decimais como string no JSON
  - Header X-Session-Expires em todo response autenticado (timestamp Unix ms)

ENUMs relevantes:
  - natureza_enum: "Debito" / "Credito" (sem acento)
  - tipo_clifor_enum: "C" / "F" / "A"

--------------------------------------------------------------------------------
  CONVENÇÕES DO FRONTEND
--------------------------------------------------------------------------------

Nomenclatura:
  - Componentes: PascalCase (ex: LancamentoForm, ClienteList)
  - Pages: PascalCase + sufixo Page (ex: LancamentoPage)
  - Services: camelCase, agrupados por domínio em api.js
  - Estilos: arquivo CSS por componente, mesmo nome (ex: lancamento.css)
  - Variáveis de domínio: português (seguindo o backend)

Padrões de estrutura:
  - Lógica de negócio e chamadas HTTP ficam nos services/
  - Componentes não chamam fetch diretamente — usam services/api.js
  - Pages são wrappers leves — não contêm lógica pesada
  - Rotas protegidas usam PrivateRoute ou AdminRoute
  - Todo CSS usa variáveis do tema — nunca cores hardcoded

Antes de criar componente novo:
  1. Verificar se já existe algo aproveitável em components/
  2. Verificar se a rota da API já existe em openapi_ai.yaml
  3. Verificar se o serviço já está em services/api.js

--------------------------------------------------------------------------------
  COMO ENVIAR CONTEXTO PARA NOVO CHAT
--------------------------------------------------------------------------------

Enviar na ordem:
  1. meu_perfil_de_trabalho_verde.txt
  2. contexto_macro_front.txt (este arquivo)
  3. fase_atual_front.txt
  4. App.jsx
  5. services/api.js + services/auth.js
  6. Componente/page específico do que for trabalhar

--------------------------------------------------------------------------------
  BACKEND — REFERÊNCIA (NÃO MODIFICAR)
--------------------------------------------------------------------------------

O backend está finalizado para o MVP. Qualquer necessidade nova do frontend
que exija mudança no backend deve ser discutida antes de implementar.

Stack: FastAPI + PostgreSQL 17.4 + SQLAlchemy + JWT com bcrypt
Caminho: C:\Codigos\AMSI_Project_Desenvolvimento\backend
Testes: 102 passando (pytest tests/)
Admin permanente: opedroschvartz@gmail.com, id=5 — nunca deletar

================================================================================